apiVersion: v1
kind: Service
metadata:
  name: ambulance-management-ambulance-ufe
spec:
  selector:
    pod: ambulance-management-ambulance-ufe-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http