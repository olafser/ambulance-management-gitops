apiVersion: apps/v1
kind: Deployment
metadata:
  name: ambulance-management-webapi
spec:
  template:
    spec:
      containers:
        - name: openapi-ui
          env:
            - name: URL
              value: /ambulance-management-openapi
            - name: BASE_URL
              value: /ambulance-management-openapi-ui